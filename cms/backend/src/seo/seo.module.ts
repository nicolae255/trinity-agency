import { Module } from '@nestjs/common';

/**
 * SEO module placeholder. Currently provides only shared DTOs.
 * Future iterations can add SeoService (sitemap generation, robots.txt,
 * structured data helpers) and SeoController here.
 */
@Module({})
export class SeoModule {}
